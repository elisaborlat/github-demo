/**
 * Takes two numbers a and b, and returns their subtraction.
 */
// Ajout d'un petit comm, on verra si il y a conflit

function subtract(a, b) {
  return a-b;
}

calculate('subtraction', subtract);
